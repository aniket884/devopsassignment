pipeline {
    agent any

    stages {
        stage('Test data') {
            steps {
                echo 'Test'
            }
        }
        stage('Build data') {
            steps {
                echo 'Build'
            }
        }
        stage('Deploy data') {
            steps {
                echo 'Deploy'
            }
        }
    }
}
